package org.calculator.user;

import org.calculator.processing.Invoker;

class EntryPanel extends FunctionsPanel{

	public EntryPanel(Ui gui, Invoker invoker, Observer observer) {
		super(gui, invoker, observer);
	}
}
